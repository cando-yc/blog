---
import { SITE } from '../consts';
---
<aside class="author-card">
  <img class="avatar" src="/cando-portrait.webp" alt={SITE.authorEn} width="88" height="88" loading="lazy" />
  <div class="ac-body">
    <p class="ac-name"><a href="/about/">{SITE.authorEn}</a><span class="ac-sub"> · {SITE.name} · AICPA</span></p>
    <p class="ac-bio">近 20 年財務、會計與稅務實戰，待過外商、中小企業和新創。現在做企業主的外部財務長：帳、稅、現金流整包接過來，讓老闆把時間花在把生意做大。</p>
  </div>
</aside>

<style>
  .author-card {
    display: flex; gap: 1.2rem; align-items: flex-start;
    border: 1px solid var(--line); padding: clamp(1.4rem, 3vw, 2rem);
  }
  .avatar {
    width: 88px; height: 88px; flex-shrink: 0;
    object-fit: cover; border-radius: 50%; border: 1px solid var(--line);
  }
  .ac-name { font-family: var(--font-serif); font-size: 1.15rem; font-weight: 500; margin: 0 0 .5rem; }
  .ac-sub { color: var(--ink-mute); font-weight: 400; font-size: .95rem; }
  .ac-bio { font-size: .92rem; line-height: 1.85; color: var(--ink-soft); margin: 0; }
  @media (max-width: 520px) {
    .author-card { flex-direction: column; gap: .9rem; }
  }
</style>
